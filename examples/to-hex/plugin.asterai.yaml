name: to-hex
functions:
  - name: convertDecimalToHex
    description: >-
      converts a decimal integer to a hexadecimal string starting with 0x
    arguments:
      - name: integer
        type: u64
        description: the physical delivery address
